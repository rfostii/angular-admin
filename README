#############################
setup project
#############################
git checkout -b without-external-dependencies
git pull origin without-external-dependencies

npm config get prefix #get path to folder with global modules
#copy all packages from node_modules to folder with global modules

#############################
run unit tests
#############################

karma start

#############################
run e2e
#############################
protractor e2e-tests/protractor.conf.js
